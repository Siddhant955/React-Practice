import React from 'react';

const EventImg = (props) => {
  return (
    <div className="">
      <img className="" src={props.source} alt="Logo" />
    </div>
  );
};

export default EventImg;
